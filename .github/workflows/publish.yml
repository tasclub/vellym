name: Publish

# GitHub Release を published にすると npm へ provenance 付きで公開する。
#
# 認証はnpmのTrusted Publishing（OIDC）を使い、publish用のtokenは保持しない。
# npm側でpackage vellymのTrusted Publisherに、このリポジトリと
# workflowファイル名publish.ymlを登録しておくこと（Environmentは未設定）。
# npm CLIは11.5.1以降が必要で、Node.js 24に同梱される版がこれを満たす。
on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # npm Trusted Publishing の認証と provenance の署名に必要
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: npm
      - run: npm ci
      - run: npm run release:check -- "${{ github.event.release.tag_name }}"
      - run: npm run typecheck
      - run: npm test
      - run: npm run pack:smoke
      - run: npx playwright install --with-deps chromium
      - run: npm run verify:core
      - id: dist-tag
        name: Select npm dist-tag
        run: |
          VERSION="$(node -p "require('./packages/vellym/package.json').version")"
          PRERELEASE="${VERSION#*-}"
          if [ "$PRERELEASE" = "$VERSION" ]; then
            TAG=latest
          else
            TAG="${PRERELEASE%%.*}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
      - name: Publish to npm
        # prereleaseは専用tag（beta等）で公開したうえで、latestも同じ版へ向ける。
        # 1.0未満の間はlatestを持たないと`npx vellym`が解決できず、
        # 見つけた利用者の最初のコマンドが失敗するため。
        run: |
          npm publish --workspace vellym --provenance --access public \
            --tag "${{ steps.dist-tag.outputs.tag }}"
          if [ "${{ steps.dist-tag.outputs.tag }}" != "latest" ]; then
            npm dist-tag add "vellym@${{ steps.dist-tag.outputs.version }}" latest
          fi
      - name: Report manual dist-tag fallback
        if: failure()
        run: |
          echo "publish後にlatestの更新だけ失敗した場合は、次を手元で実行する:" >&2
          echo "  npm dist-tag add vellym@${{ steps.dist-tag.outputs.version }} latest" >&2
