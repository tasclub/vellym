# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

1.0.0 未満の期間は、マイナー版の間でも互換性のない変更が入りうる。
正本YAMLの形式を変更する場合は、`vellym migrate` による明示的な移行手段を用意し、
既存ファイルを暗黙に書き換えない。

## [Unreleased]

### 追加

- 一覧に列として出す項目を、チケット管理の定義が宣言するようにした
  （`fields[].listColumn`）。「先頭2項目を主列にする」暗黙の規則をやめた。項目を
  1つ足すたびに一覧の見え方が変わらないようにする。タイトルとステータスは
  `fields`に書くものではなく、常に出る組み込みの列である。
- 新しいチケットを画面から作るときの初期値を、**定義の並び順から決める**ように
  した。初期値のための宣言（`default`のようなキー）は持たない。ステータスは
  `statuses`の先頭、単一選択は`options`の先頭、真偽値は偽、複数選択は空とし、
  それ以外の型はキーごと書かない。並び順と初期値を二重に持たせない。
- 必須（`fields[].required`）を、**画面から保存する操作だけ**を止めるものとして
  定めた。開くこと、編集画面へ入ること、やめること、YAMLを直接直すことは妨げない。
  `validate`も`build`も必須の欠落で失敗しない。空のまま保存しようとしたときは、
  最初の未入力項目へフォーカスを移す。正本がファイルである以上、画面が保証できる
  のは「画面から入れたものは揃っている」ことまでである。
- コマンドの入力を型付きにした（`PluginInputValue`）。すべてを文字列へ潰すと、
  真偽値のfalseと未入力、数値の0と空欄がプラグイン側で区別できない。
  対象によって尋ねることが変わる場合のため、`inputs`を関数でも宣言できるようにした。
- 一覧に列ごとの絞り込みを追加した。対象はステータス、`listColumn`の項目、
  `metadata.labels`。選択肢のある列は完全一致、それ以外は部分一致とする。
  **本文の検索は置かない。** 全文検索が受け持つ。現在の絞り込みと並び順は
  個人設定として保持し、正本YAMLへ書かない。
- 並べ替えの比較を列の型に従うようにした。数値は数として、真偽値は真偽として
  比べる。空欄は末尾へ寄せる。文字列比較のままでは`9`が`10`の後ろへ並んだ。
- 一覧からの一括編集を追加した。行を複数選び、ステータス・単一選択の項目・
  アーカイブへの移動をまとめて行える。**1件ずつ通常の保存経路を通す**ため、
  非破壊往復・realpath境界・hash競合検出がそのまま効く。途中で失敗しても
  成功した分は残し、成功件数と失敗件数を分けて示す。失敗した行は選択に残る。
  一括変更できる列はプラグインが宣言する（`editPath`）。hostが推測しない。
- ステータスと項目の定義編集に、行の上下移動を追加した。並び順が表示順であり、
  先頭が新規作成時の初期値になるため、並べ替えられなければどちらも決められない。
- `metadata.labels`を索引行へ載せた。`キー=値`を並べた1つの値として持ち、
  一覧では列を増やさずに畳んで見せ、絞り込みだけできるようにする。
- プラグインの公開契約`@vellym/plugin-api`を追加した。host API、プラグインが受け取る
  抽出済みレコード、一覧・詳細のdescriptor、`package.json`の`vellym`フィールドの
  JSON Schemaを持つ。型と定数だけで、実行時依存は無い。
  Core側がこの契約を実装する向きに依存を固定し、内部モジュールを公開契約にしない。
- `vellym.config.yaml`の`plugins`に書かれたnpmパッケージを解決し、`activate(host)`を
  呼んで貢献点を集めるようにした。解決は設定ファイルのあるディレクトリ起点で行い、
  CLI自身の依存へは降りない。解決失敗、manifest不正、`engines.vellym`の版域外、
  `activate`の例外はいずれも警告の診断になり、`vellym validate`も`vellym dev`も
  失敗させない。ブラウザ側の配信と画面への反映は未実装である。
- 公式チケットプラグイン`@vellym/tickets`のNode側を追加した。チケット管理は
  **工程ごとに複数持てる**。`TicketTracker`を文書階層の任意の位置へ置くと、
  そのフォルダ配下のチケットがその管理に属する（最も近い祖先が所有する）。
  ステータスと項目の定義は`TicketTracker`1ファイルにまとまり、チケット管理ごとに
  異なる一覧・詳細のdescriptorが組み立てられる。`TicketTracker`は文書ツリーへ出し、
  個々のチケットは出さない。定義と実データの食い違い（定義外のステータス、定義外の
  項目、必須項目の欠落、型の不一致、所属が無い）はすべて警告になり、チケットは
  読めて編集できる状態を保つ。画面への反映はブラウザ側が未実装のため、まだ
  表示されない。
- 有効なプラグインが登録したkindを、repositoryの読み込みが既知として扱うように
  した。`vellym validate`、`vellym build`、`vellym dev`のいずれでも、そのkindは
  `UNKNOWN_RESOURCE_KIND`の警告を出さずに読み進める。共通契約の範囲だけを解釈し、
  種別固有の`spec`は原文のまま保持する点は変わらない。`plugins`を空にすれば同じ
  ファイルが未知kindの扱いへ戻る。
- プラグインが宣言したkindを文書ツリーへ出せるようにした（`showInDocumentTree`）。
  チケット管理（`TicketTracker`）は工程の階層に現れ、個々のチケットは現れない。
  解釈できるプラグインがあるkindは「解釈できない内容」の集約にも数えない。
- プラグインが登録した索引行の生成を、repositoryの読み込みへ組み込んだ。
  projectorを登録したkindは大量データとして扱い、完全なリソースを常駐させず
  索引行だけを保持する。登録の無いプラグインkindは定義として扱い、projectorを
  組み立てる文脈から読める。定義が変わった場合は索引行を作り直す。
- `_`で始まるフォルダとその配下を、文書ツリーへ出さないようにした。`_index.yaml`と
  `_archive/`で既に使っている印を、フォルダ名の規約として広げたものである。
  **隠すだけで読み込みからは外さない。** 全文検索と内部リンクからは到達でき、
  ファイルにも触れない。
- 一覧に「完了を含める」の切替と、チケット・チケット管理の作成を追加した。
  既定では完了だけを隠す。ステータスが未設定のチケットや定義外のチケットが、
  数え上げ漏れで一覧から消えないようにしている。
- プラグインのコマンドが、起こす前に利用者へ尋ねる入力を宣言できるようにした
  （`inputs`）。チケット管理の作成は名前を尋ねてから作る。チケットの作成は
  何も尋ねず、初期値だけのチケットを作ってそのまま編集画面を開く。
- 詳細に、属するチケット管理へ戻る導線を追加した（`parent`）。紙の外・いちばん上に
  「← 一覧へ戻る」として置き、戻り先の名前は添え書きにする。戻り先の名前を
  そのまま文言にすると、一覧へ戻るのかその名前の文書へ行くのかが読めない。
- チケット管理（`TicketTracker`）を文書ツリーの追加メニューから作れるようにした。
  プラグインは`placement: "document-tree"`でそこへ並ぶことを宣言する。作成直後は
  設定画面へ移り、ステータスと項目を定義できる。定義が空のまま放置させない。
- ステータスと項目の定義を画面から編集できるようにした。契約に繰り返し構造の宣言
  （`type: "group"`）を追加し、行の追加と削除を汎用レンダラが受け持つ。
- チケットの閲覧と編集を、通常のPageと同じ画面・同じ操作にした。**参照は読むだけで、
  編集はタイトル・ステータス・項目・本文を1つの画面で直し、1回の保存でまとめて
  書く。** 属性だけが別の保存経路を持たない。**新規作成のための画面を別に持たず、
  作成を押すと初期値だけのチケットができてそのまま編集画面が開く。**
  本文の有無で編集を塞がない。チケットは属性とタイトルが本体である。
- ビューを切り替える導線を、通常のPageの編集ボタンと同じ位置（見出し横）へ置いた。
- プラグインが出した注意を、その資源の画面で見せるようにした。一覧では行に件数を
  示し、詳細では内容を並べる。**警告で「読み込めない文書があります」を出さない。**
  この表示は読み込みに失敗したとき（error）だけにする。
- 同じkindへ複数のビューを登録できるようにした。切り替えの表示名はプラグインが
  宣言する（`label`）。hostが名前を推測しない。
- プラグインが宣言した詳細を画面へ描くようにした。チケットを開くと、見出しと本文の
  間にステータスと項目が並ぶ。保存は通常のPage保存経路を通るため、非破壊往復・
  realpath境界・hash競合検出がそのまま効く。定義から消えた項目の値は読み取り専用で残す。
- `spec`直下の値を書き戻す保存経路を追加した（`specValues`）。`blocks`・`locale`・
  `translations`は指定できない。Coreが解釈する領域は専用の経路のままにする。
- 保存後の検証を種別ごとに分けた。`kind: Page`以外はPageスキーマで検証せず、
  共通契約までを検証する。種別固有スキーマとの食い違いで保存を止めない。
- プラグインが宣言した一覧を画面へ描くようにした。`TicketTracker`を文書ツリーから
  開くと、本文の代わりにその配下のチケット一覧になる。列、選択肢の表示名、並べ替えは
  プラグインのdescriptorから作り、チケット専用のコンポーネントは持たない。
  対応するビューが無いリソースは通常の文書として表示する。
- `vellym init`が`package.json`を生成するようにした。`name`（ディレクトリ名から
  正規化）、`version`、`private`、`type`だけの最小構成で、既存ファイルがある場合は
  内容を検査せず触らない。
- **`@vellym/ui`を公開した。** 本体が持つUI部品（Button・Dialog・DataTable・
  FieldInput・Icon・KindIcon・RequiredMark）の**型だけを配るパッケージ**である。
  実装は本体が持ち、ブラウザではimport mapが`/assets/vellym-ui.js`へ向ける。
  プラグインはexternalとしてビルドし、実体を本体から受け取る。同梱すると
  部品が二重になり、Reactのインスタンスも分かれてhooksが壊れる。
  **宣言は実装から生成する。** 手で書かないので、実装と宣言が離れない。
  propsは縛らない。`Button`はHTMLの属性を素通しし、`DataTable`はセルの中身を
  コールバックで受けるため、プラグインが任意の属性と見せ方を足せる。
- プラグインが自分で描いた画面から正本へ書き戻せるようにした
  （`PluginRenderContext.save`）。**Coreの保存経路をそのまま通す**ので、
  非破壊往復・realpath境界・hash競合の検出が同じく効く。あわせて索引行を
  渡し（`rows`）、使用中の値の件数をプラグインが数えられるようにした。

### 変更

- **ブラウザ側の描画契約をmountにした（互換性のない変更）。**
  `PluginComponent`（`(props) => unknown`）を廃止し、
  `mount(element, context) => { update?(context), unmount() }` へ置き換えた。
  `VellymBrowserPluginHost.registerViewRenderer`はcomponentではなく
  rendererを受け取る。**プラグインをReactで書く必要が無くなった。**
  Vanilla、Svelte、Vue、Preactのいずれでも書ける。特定のUIライブラリへの
  依存を第三者へ強制しない。多重mountをしないこと、`unmount`の後に
  `update`を呼ばないこと、`unmount`の完了を待たないことをhost側の規約とし、
  型のコメントに明記した。
- Reactで書く作者のために`@vellym/plugin-api/react`を足し、
  `reactRenderer(Component)`を置いた。Reactのcomponentをmount契約へ包む薄い
  層である。`react`と`react-dom`は**optionalなpeerDependency**とし、
  このsubpathを使わないプラグインへはReactを要求しない。契約パッケージの
  既定のエントリは依存ゼロのままである。
- チケットの一覧・詳細・作成・定義編集の見た目を、引き継ぎ資料の紙面へ揃えた。
  角丸と影を外し、罫線と余白と字の大きさだけで区切る。一覧は13px・行の罫線を
  細く・題名を太く、絞り込みは沈めた地の帯（`FILTER`）、選択中の帯は2倍の輪郭で
  示す。詳細と作成の入力はどちらも「ラベル左150px・入力右」の同じ格好にした。
  作るときと直すときで同じ項目が別の顔を持たないようにする。
- 選択肢に記号を宣言できるようにした（`PluginSelectOption.symbol`）。
  **状態を色だけで伝えない。** 記号と文言の両方を出す。チケットのステータスは
  `category`からだけ記号を決め（未完了は`□`、完了は`✓`）、ステータスごとの記号を
  推測しない。利用者が付けた表示名から意味を読み取ると、「保留」を完了として
  扱うような誤りが起きる。
- 一覧に件数を出すようにした（`6件（全12件中）`）。絞り込みで消えた分に気づける
  ようにする。
- 一覧の行で、読み込めなかったものと、値は読めていて注意があるだけのものを
  区別して示すようにした。同じ見た目にすると、直すべきものが埋もれる。
- 同じフォルダに`TicketTracker`が2つある場合に警告を出すのをやめた。
  `metadata.name`の昇順で先頭を使う点は変わらない。両方を解釈する方法が無い以上、
  診断を1つ足しても利用者にできることが増えない。
- 項目の現在値をどの索引行キーから読むかを、プラグインが宣言するようにした
  （`PluginFieldDescriptor.valueKey`、`PluginDetailViewDescriptor.undeclaredKeyPrefix`）。
  汎用レンダラが`path`からキーを組み立てるのをやめた。あるプラグインの命名規約が
  hostへ染み出していた。
- プラグインの画面では、本文についてのCoreの理由（「編集できる本文ブロックが
  ありません」）を出さないようにした。一覧や属性の画面では意味を持たない。
- 既定の絞り込みを、列ごとの絞り込みと同じ帯へ「外せる切替」として置くようにした
  （`PluginFilterDescriptor.toggleLabels`）。同じことを二か所で操作させない。
  **切替の文言は、押した結果ではなく「いま何が表示されているか」を出す**
  （「未完了のみ」⇄「すべて表示中」）。押しても位置も形も変わらないため、
  文言が変わらないと入と切のどちらなのかが読めない。文言はプラグインが
  入と切の両方を宣言する。hostは「未完了」のような種別の語を持たない。

- チケット管理の設定を保存したときに、保存できた旨が出るようにした。読み直しが
  保存状態とメッセージを消すため、**読み直したあとで結果を示す**順序へ直した。
  失敗したときも保存状態を`失敗`にする。以前は文言を入れるだけで画面に出なかった。
- ビューを切り替えたとき、直前に走っていた読み込みの応答で表示が上書きされない
  ようにした。設定画面へ入った直後に一覧へ戻されることがあった。
- 設定画面の「一覧へ戻る」が効かなかったのを直した。設定と一覧は**同じ資源の
  別のビュー**であり、読み込みは開いている資源の変化で起きるため、同じ名前へ
  移動しても何も起きなかった。ビューを戻して読み直すようにした。

### 削除

- **太った宣言契約を3件剥がした（互換性のない変更）。** チケット1機能のために
  契約へ足したものを10件見直し、3件を落として7件を残した。
  - `PluginGroupFieldDescriptor`（`type: "group"`）— 繰り返しの中の繰り返しを
    表せず、契約に入れ子を足す方向は採らないと決めた。プラグインが自分で描く
    経路が通ったので役目を終えた。定義の編集画面はプラグイン側が描く。
  - `PluginCommandInput.initialValue` — 使う者が居なかった。
  - `inputs`の関数形（`PluginCommandInputsProvider`）— 同じく使う者が居なかった。
    対象ごとに尋ねることを変えたくなったら、宣言を増やさずプラグイン側の
    画面で解く。

  残した7件（`PluginInputValue`・`symbol`・`valueKey`2つ・`editPath`・
  `undeclaredKeyPrefix`・`hasBlocks`）は、**宣言で済ませることで
  アクセシビリティ・日英表示・静的版対応・キーボード操作がプラグインへ
  自動的に及ぶ**もの、または種別の性質を表すものである。剥がすと
  プラグインが各自で作り直すことになり、必ず劣化する。

- host側の辞書から`plugin.includeClosed`（「完了を含める」）を削除した。
  種別の語がCoreの辞書に入っていた。文言はプラグインが宣言する。

- ビューの`navigation`（サイドバーへ出すナビゲーション項目）の宣言を削除した。
  文書ツリーに現れるものをもう一か所へ並べても、到達経路が二重になるだけである。
  どのチケット管理があるかを見せるのは一覧側の責務とする。

## [0.3.0-beta.1] - 2026-08-14

### 追加

- 本文の内部Pageリンクに、Obsidian互換のwiki link記法`[[...]]`を導入した。
  `[[target]]`、`[[target|表示名]]`、`[[target#見出し]]`とその組み合わせを解釈し、
  参照先は`metadata.name`→`metadata.slug`→titleの順に解決する。
  逆参照（このPageを参照しているPage）とリンク切れ診断を、動的版と静的版の
  双方で提供する。外部URL・mailto・相対path・画像は通常のMarkdownリンクのまま。
- Coreが解釈できない`kind`のファイルを、エラーではなく警告として読み進めるようにした。
  共通契約（`apiVersion`／`kind`／`metadata`／`spec.locale`／`translations`／`blocks`）
  だけを解釈し、種別固有の`spec`は解釈せず原文のまま保持する。全文検索・内部リンク・
  逆参照・多言語projectionの対象には含め、文書ツリーには出さない。
  プラグインを外してCoreだけになっても壊れないようにするための変更である。
- 初期セットアップの構成テンプレートを`setup-packs/`配下の外部定義へ切り出し、
  生成候補を階層ツリーで選択・絞り込みできるようにした。

### 変更

- `vellym init`の引数を`--size`と`--method`にした。規模（personal／small-team／
  medium-large）と開発方式（agile／hybrid／waterfall）から構成を導出する。
  従来の`--profile`、`--template`、`--plan`は廃止した。
- HTTP応答と静的データの封筒の`schemaVersion`を`apiSchemaVersion`へ改名した。
  `vellym-build.json`は`buildSchemaVersion`、setup pack定義は`packSchemaVersion`
  へ改名した。同じ名前で異なる契約を指していたものを分離した。
  `vellym.config.yaml`の`schemaVersion`は利用者が書く項目のため変更していない。

### 削除

- 本文リンクの`[表示名](#page=<slug>)`形式を廃止した。内部Pageリンクの記法は
  `[[...]]`ひとつに統一する。互換読み取りは実装していないため、既存の本文は
  `[[...]]`へ書き換える必要がある。
- `spec.documentType`を廃止した。当初は表示形式の判別子として確保した項目だが
  定義が定まらないまま分類ラベルとして使われており、その役割は`kind`が担う。
  唯一の表示先だった本文上部の種別表示も削除した。
- `spec.blocks[].format`を廃止した。`commonmark`固定で分岐に使われておらず、
  別形式を受け入れる計画もない。Mermaidや数式はCommonMarkのコードブロックで
  表現できるため、この廃止と競合しない。
- 生成物へのprovenance annotations（`setup-pack-id`、`template-id`、
  `information-area-id`等）の書き込みをやめた。生成後は利用者が自由に書き換える
  ため、生成元の記録は実態と乖離する。

`documentType`と`format`は解釈しなくなるだけで、既存YAMLに残っていても
未知の項目として保持・無視される。ファイルの一括修正は不要である。
`apiVersion`は`vellym.tasclub.com/v1`のまま据え置く。今回の変更はいずれも
既存ファイルを壊さない緩和であり、版を上げる理由が発生していない。

### その他

- `spec.blocks`を任意にした。本文を持たない種別を許すため。

## [0.2.0-beta.1] - 2026-08-13

### 変更

- 新規に生成するPage・Folderの`apiVersion`を`vellym.tasclub.com/v1`にした。
  `init`、ブラウザの初期セットアップ、画面からのPage・Folder作成が対象。
  既存の`v1alpha1`はこれまでどおり読み込み・編集・検索でき、
  移行する場合は`vellym migrate --to v1`を明示的に実行する。
- 公開パッケージの`engines.node`を`>=22.12.0`にした。
  JSON module（`import ... with { type: "json" }`）が安定して利用できる最小版に合わせた。
  ビルドターゲットも`node22`へ揃えた。

- 文書形式の進化規則を定め、Page・Folderのschemaを版ごとに分けず1枚に統合した。
  `apiVersion`はenumで対応版を受ける。追加（新しいblock種別、任意項目）では版を上げず、
  版を上げるのは破壊的変更のときだけとする。変換は直前の版からの一段だけを提供し、
  同時にサポートする版は2つまでとする。詳細は「文書形式と互換性」ガイドを参照。
  公開schema URL（`/schemas/v1/...`）は従来どおり版ごとに分かれ、その版だけを受け付ける。

### セキュリティ

- `dev`をloopback（既定の`127.0.0.1`）へbindしている間、`Host`ヘッダを
  `localhost`・`127.0.0.1`・`[::1]`に限定するようにした。
  外部サイトがDNS rebindingでローカルのAPIへ到達する経路を塞ぐ。
- `dev --host 0.0.0.0`で起動したとき、認証がないこと、到達できる相手が
  文書の閲覧・編集・構成変更を行えることを起動時に警告するようにした。

### 修正

- 静的ファイル配信のContent-Typeが`.js`・`.css`・HTMLしか判別せず、
  画像・アイコン・フォントを`text/html`として返していた問題を修正した。
- 存在しないアセットに対してSPAの`index.html`をHTTP 200で返していた問題を修正した。
  拡張子を持つ要求は404を返す。

### 追加

- npmのdist-tagは、prereleaseでも`latest`を同じ版へ向ける。`npm install vellym`と
  `npx vellym`が解決できる状態を保つため。版を明示する場合は`vellym@beta`を使う。
- npmの公開をTrusted Publishing（OIDC）へ移行し、publish用のtokenを保持しないようにした。
- 導入コマンドの案内がdist-tagと一致しない場合、publishを中止するようにした。
  README（2件）と日英の「はじめかた」の`npx vellym@<tag>`を検査する。
- 配布物へ`THIRD-PARTY-NOTICES.md`を同梱するようにした。
  `cli.mjs`とUI bundleへ取り込む第三者packageのライセンス本文を、
  `package-lock.json`のproduction依存から生成する。

## [0.1.0-alpha.2] - 2026-08-02

### 追加

- package、CLI、静的生成、tarballのバージョンを一元化し、
  tag/version検査、pack smoke、provenance付きpublishのgateを追加した。
- GitHub Issue Form、セキュリティポリシー、コントリビューションガイドを追加した。
- 日英の公式サイト、利用ガイド、公開schemaを生成するようにした。
- `product-planning` profile（日英6文書）を追加した。
- Page・Folderの`v1` schemaと`vellym migrate --to v1`を追加した。

### 変更

- 4段階の初期セットアップと、Node.js 22／24対応の文書を整合させた。

## [0.1.0-alpha.1] - 2026-08-01

### 追加

- 初回公開。CLI（`init`／`dev`／`validate`／`build`）、
  ブラウザでの閲覧・編集・構成管理・全文検索、閲覧専用の静的サイト生成。

[Unreleased]: https://github.com/tasclub/vellym/compare/v0.3.0-beta.1...HEAD
[0.3.0-beta.1]: https://github.com/tasclub/vellym/compare/v0.2.0-beta.1...v0.3.0-beta.1
[0.2.0-beta.1]: https://github.com/tasclub/vellym/compare/v0.1.0-alpha.2...v0.2.0-beta.1
[0.1.0-alpha.2]: https://github.com/tasclub/vellym/compare/v0.1.0-alpha.1...v0.1.0-alpha.2
[0.1.0-alpha.1]: https://github.com/tasclub/vellym/releases/tag/v0.1.0-alpha.1
