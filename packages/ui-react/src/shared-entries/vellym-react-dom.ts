/** import mapの`react-dom`と`react-dom/client`が指す固定名エントリ */
export {
  createPortal,
  flushSync,
  preconnect,
  prefetchDNS,
  preinit,
  preload,
  version
} from "react-dom";
export { createRoot, hydrateRoot } from "react-dom/client";
